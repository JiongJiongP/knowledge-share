# 企业内部知识分享平台 - 设计规格

**版本**：v3.0
**日期**：2026-05-07
**状态**：进行中

***

## 1. 项目概述

### 1.1 项目目标

构建一个企业内部知识分享、检索、评论平台，支持多种内容形式、高效的全文检索与语义搜索能力。

### 1.2 核心功能范围

- 内容发布：支持 Markdown、PPT（在线预览/下载）、外部链接、内部引用
- 群组管理：公开群组、申请审批、内容分发
- 全文检索 + 语义搜索：关键词匹配 + AI 向量相似度检索
- 用户权限：SSO/账号登录、组织架构集成、角色权限体系
- 标签体系：管理员统一管理的全局标签 + 单内容多标签 + 支持标签搜索
- 收藏功能：收藏/取消收藏 + 收藏列表展示 + 跳转阅读
- 消息通知系统：站内通知 + 多场景触发 + 通知管理
- **评论与互动系统：评论发布/回复 + 点赞 + 举报 + 审核 + 敏感词过滤 + @提及**
- **内容生产工具链：历史版本管理 + 审核工作流 + 内容模板 + 定时发布**
- **数据分析看板：内容趋势 + 热门排行 + 搜索热词 + 群组活跃度**

### 1.3 非功能范围

- 移动端：**不纳入**
- 邮件/短信/IM推送：**不纳入**
- 多人协同编辑：**不纳入**
- 知识图谱：**不纳入**

***

## 2. 系统架构

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                      前端 (Vue PC Web)                   │
└─────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────┐
│                    API Gateway / 网关                   │
└─────────────────────────────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┬───────────────────┐
        ▼                   ▼                   ▼                   ▼
┌─────────────┐     ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  用户权限模块 │     │ 内容发布模块 │     │ 群组管理模块 │     │ 评论互动模块 │
└─────────────┘     └─────────────┘     └─────────────┘     └─────────────┘
        │                   │                   │                   │
        └───────────────────┼───────────────────┴───────────────────┘
                            ▼
┌─────────────────────────────────────────────────────────┐
│                   标签体系模块                           │
│   (全局标签管理 + 内容打标 + 标签搜索)                    │
└─────────────────────────────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┬───────────────────┐
        ▼                   ▼                   ▼                   ▼
┌─────────────┐     ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  收藏模块    │     │ 通知模块     │     │  搜索服务模块 │     │ 数据分析模块 │
└─────────────┘     └─────────────┘     └─────────────┘     └─────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────┐
│  文件服务 (自建) │ AI服务 (Embedding) │ 消息队列 (RabbitMQ) │
└─────────────────────────────────────────────────────────┘
```

### 2.2 技术选型

| 组件           | 推荐方案                 | 说明            |
| ------------ | -------------------- | ------------- |
| 前端框架         | Vue 3 + Element Plus | 生态成熟          |
| 后端框架         | Spring Boot          | Java 系首选      |
| 数据库          | MySQL 8.0            | 关系型存储         |
| 全文检索         | Elasticsearch 8.x    | 成熟稳定          |
| 向量数据库        | Qdrant               | 轻量、易部署、支持云原生  |
| AI Embedding | M3e 或 BGE            | 开源中文优化，支持私有部署 |
| 文件存储         | MinIO / 本地 FS        | S3 兼容，部署灵活    |
| 缓存           | Redis                | Session、热点数据  |
| 消息队列         | RabbitMQ             | 异步解耦、事件驱动    |

### 2.3 后端架构：DDD 模块化单体

#### 2.3.1 Maven 模块拆分（7 个业务模块 + 1 个启动模块）

```
knowledge-platform/
├── knowledge-common/          # 公共基础设施（DTO、工具类、异常定义）
├── knowledge-user-auth/       # 用户 + 部门 + 角色权限
├── knowledge-content/         # 内容发布 + 版本管理 + 审核工作流 + 标签
├── knowledge-social/          # 评论 + 群组 + 收藏
├── knowledge-notification/    # 通知（独立，MQ 消费者多）
├── knowledge-search/          # 搜索（独立，ES/Qdrant 集成重）
├── knowledge-file/            # 文件服务（独立，MinIO 集成）
├── knowledge-analytics/       # 数据分析（独立，查询模型不同）
└── knowledge-web/             # Spring Boot 启动入口 + Controller 层
```

#### 2.3.2 模块依赖关系

```
knowledge-web (启动入口)
    ├── knowledge-user-auth
    ├── knowledge-content ──────→ knowledge-file
    ├── knowledge-social ───────→ knowledge-content, knowledge-user-auth
    ├── knowledge-notification ─→ knowledge-user-auth
    ├── knowledge-search ───────→ knowledge-content
    ├── knowledge-analytics ────→ knowledge-content, knowledge-user-auth
    └── knowledge-file
```

#### 2.3.3 每个模块的 DDD 内部结构

```
knowledge-xxx/
├── domain/           # 领域层
│   ├── model/        # 聚合根、实体、值对象
│   ├── repository/   # 仓储接口
│   ├── service/      # 领域服务
│   └── event/        # 领域事件定义
├── application/      # 应用层
│   ├── service/      # 应用服务（编排、事务）
│   └── dto/          # 应用层 DTO
├── infrastructure/   # 基础设施层
│   ├── repository/   # 仓储实现（JPA/MyBatis）
│   ├── mq/           # 消息队列生产者/消费者
│   └── external/     # 外部服务调用
└── interfaces/       # 接口层（仅 knowledge-web 模块有 Controller）
```

#### 2.3.4 模块间通信规则

| 通信方式 | 适用场景 |
|---------|---------|
| **直接依赖调用** | 查询类操作（如内容模块查标签名称）、同步校验（如发布内容时校验群组存在） |
| **领域事件（MQ）** | 异步通知（内容发布→通知群成员）、索引更新（内容变更→ES/Qdrant 重建）、数据采集（浏览/搜索→埋点入库） |

### 2.4 消息队列事件设计

#### 2.4.1 Topic 规划

| Topic | 生产者 | 消费者 | 说明 |
|-------|--------|--------|------|
| `content.event` | content | notification, search, analytics | 内容发布/更新/删除 |
| `comment.event` | social | notification, analytics | 评论发布/回复/@提及 |
| `group.event` | social | notification, analytics | 入群申请/审批/群组创建 |
| `audit.event` | content | notification | 审核通过/驳回 |
| `user.action` | web（埋点） | analytics | 浏览/搜索/收藏/下载行为 |
| `file.process` | content | file | PPT 解析、图片压缩等异步处理 |

#### 2.4.2 关键事件流示例

**内容发布事件流：**

```
用户点击发布
    │
    ▼
content 模块：保存内容 → 发送 ContentPublishedEvent
    │
    ├──→ notification 消费：给群组成员发通知
    ├──→ search 消费：生成向量 → 写入 Qdrant + 更新 ES 索引
    └──→ analytics 消费：记录发布行为
```

**评论 @提及事件流：**

```
用户发布评论（含 @张三）
    │
    ▼
social 模块：保存评论 → 发送 CommentCreatedEvent（含 mentionedUserIds）
    │
    ├──→ notification 消费：给被 @ 的用户发通知
    └──→ analytics 消费：记录评论行为
```

#### 2.4.3 消息可靠性策略

| 策略 | 说明 |
|------|------|
| **生产者确认** | 发送消息后等待 Broker ACK，失败重试 3 次 |
| **消费者手动 ACK** | 业务处理成功后再确认，失败进入死信队列 |
| **幂等消费** | 消费者通过 eventId 去重，防止重复处理 |
| **本地消息表** | 关键业务（如内容发布）先写本地事件表，再发 MQ，定时任务补偿 |

***

## 3. 数据模型

### 3.1 用户权限模块

**实体**：

- `User`：用户基本信息、SSO标识、所属部门
- `Department`：部门树形结构
- `Role`：角色定义（系统管理员、普通用户）
- `Permission`：权限点
- `GroupMember`：群组成员（用户、群组、群组内角色、入群时间、审批状态）

**关系**：

- 用户 N:1 部门
- 用户 N:N 角色（全局角色）
- 用户 1:N GroupMember（用户在多个群组中可以有不同角色）
- GroupMember N:1 Group（群组）
- 角色 N:N 权限

**全局角色**：

| 角色 | 说明 |
|------|------|
| **普通用户** | 默认角色，所有登录用户自动拥有 |
| **系统管理员** | 全站管理：用户管理、部门管理、标签管理、系统配置、群组创建审批 |

**群组内角色**：

| 角色 | 说明 |
|------|------|
| **群主** | 群组创建者，拥有群组内管理权限 |
| **成员** | 群组普通成员 |

### 3.2 内容发布模块

**实体**：

- `KnowledgeContent`：知识内容（标题、正文、内容类型、发布状态、创建者、创建时间、发布时间）
- `ContentGroupRelation`：内容-群组关联（多对多）
- `ContentTag`：内容标签

**内容类型枚举**：

- `MARKDOWN`：Markdown 富文本
- `PPT_FILE`：PPT 文件（支持上传下载和在线预览）
- `EXTERNAL_URL`：外部链接
- `INTERNAL_REF`：内部引用

**发布状态枚举**：

- `DRAFT`：草稿
- `PUBLISHED`：已发布

### 3.3 群组管理模块

**实体**：

- `Group`：群组（名称、描述、创建者、可见性设置、创建时间）
- `GroupMember`：群组成员（用户、群组、角色、入群时间、审批状态）

**可见性设置**：

- `PUBLIC`：所有内容对全员可见
- `PARTIAL`：群主设置哪些内容对外可见

**审批状态枚举**：

- `PENDING`：待审批
- `APPROVED`：已批准
- `REJECTED`：已拒绝

### 3.4 标签体系模块

**实体：Tag（标签）**

```
Tag {
  id: Long           // 主键
  name: String       // 标签名称（唯一，如"技术"、"产品"）
  color: String      // 标签颜色（如"#FF5733"）
  createdAt: Date    // 创建时间
  createdBy: Long    // 创建人（管理员ID）
}
```

**关联表：ContentTagRelation（内容-标签关联）**

```
ContentTagRelation {
  id: Long
  contentId: Long    // 知识内容ID
  tagId: Long        // 标签ID
  createdAt: Date    // 打标时间
}
```

**说明**：

- 标签为管理员统一管理的全局标签
- 单内容支持多标签
- 用户在发布/编辑内容时可选择标签

### 3.5 收藏模块

**实体：Favorite（收藏）**

```
Favorite {
  id: Long           // 主键
  userId: Long       // 用户ID
  contentId: Long    // 收藏的内容ID
  createdAt: Date    // 收藏时间
}
```

**约束**：

- 用户ID + 内容ID 联合唯一索引，防止重复收藏
- 收藏列表仅用户本人可见

### 3.6 消息通知模块

**实体：Notification（通知）**

```
Notification {
  id: Long           // 主键
  userId: Long       // 通知接收用户ID
  type: String       // 通知类型
  title: String      // 通知标题
  content: String    // 通知内容摘要
  relatedId: Long    // 关联实体ID（如内容ID、申请ID等）
  relatedType: String // 关联类型（如CONTENT、GROUP_APPLICATION等）
  isRead: Boolean   // 是否已读
  createdAt: Date    // 创建时间
}
```

**通知类型枚举：**

| type | 说明 | relatedType |
|------|------|-------------|
| `CONTENT_PUBLISHED` | 内容发布通知 | CONTENT |
| `COMMENT_REPLY` | 评论/回复通知 | COMMENT |
| `COMMENT_MENTION` | 评论@提及通知 | COMMENT |
| `GROUP_JOIN_APPROVED` | 入群申请通过 | GROUP_APPLICATION |
| `GROUP_JOIN_REJECTED` | 入群申请被拒 | GROUP_APPLICATION |
| `GROUP_JOIN_APPLY` | 新成员申请（群主） | GROUP_APPLICATION |
| `GROUP_CREATE_APPROVED` | 群组创建申请通过 | GROUP_CREATE_APPLICATION |
| `GROUP_CREATE_REJECTED` | 群组创建申请被拒 | GROUP_CREATE_APPLICATION |
| `GROUP_CREATE_APPLY` | 新的群组创建申请（管理员） | GROUP_CREATE_APPLICATION |
| `CONTENT_UPDATED` | 内容更新通知 | CONTENT |
| `CONTENT_AUDIT_APPROVED` | 内容审核通过 | AUDIT |
| `CONTENT_AUDIT_REJECTED` | 内容审核驳回 | AUDIT |
| `SYSTEM_ANNOUNCEMENT` | 系统公告 | SYSTEM |

### 3.7 评论互动模块

**实体：Comment（评论）**

```
Comment {
  id: Long              // 主键
  contentId: Long       // 被评论的内容ID
  parentId: Long        // 父评论ID（NULL=一级评论）
  replyToId: Long       // 被回复的评论ID
  replyToUserId: Long   // 被回复的用户ID
  userId: Long          // 评论者ID
  body: String          // 评论正文（富文本/Markdown）
  likeCount: Int        // 点赞数
  status: String        // PUBLISHED/DELETED/HIDDEN
  auditStatus: String   // PENDING/APPROVED/REJECTED
  createdAt: Date       // 创建时间
  updatedAt: Date       // 更新时间
}
```

**实体：CommentLike（评论点赞）**

```
CommentLike {
  id: Long
  commentId: Long
  userId: Long
  createdAt: Date
}
```

**约束**：commentId + userId 联合唯一索引

**实体：CommentMention（评论@提及）**

```
CommentMention {
  id: Long
  commentId: Long
  mentionedUserId: Long
}
```

**实体：SensitiveWord（敏感词）**

```
SensitiveWord {
  id: Long
  word: String          // 敏感词
  category: String      // 分类（GENERAL/POLITICAL/ADVERTISING）
  createdAt: Date
}
```

**约束**：word 唯一索引

### 3.8 内容生产工具链模块

**实体：ContentVersion（历史版本）**

```
ContentVersion {
  id: Long
  contentId: Long
  versionNumber: Int
  title: String
  body: String
  changeSummary: String   // 变更摘要
  createdBy: Long
  createdAt: Date
}
```

**约束**：contentId + versionNumber 联合唯一索引

**实体：AuditRecord（审核记录）**

```
AuditRecord {
  id: Long
  targetType: String      // CONTENT / COMMENT
  targetId: Long
  submitterId: Long       // 提交人
  reviewerId: Long        // 审核人
  status: String          // PENDING/APPROVED/REJECTED
  rejectReason: String    // 驳回原因
  submittedAt: Date
  reviewedAt: Date
}
```

**实体：ContentTemplate（内容模板）**

```
ContentTemplate {
  id: Long
  name: String
  description: String
  contentType: String     // MARKDOWN/PPT_FILE/EXTERNAL_URL
  body: String            // 模板内容
  isSystem: Boolean       // 是否系统预置模板
  createdBy: Long
  createdAt: Date
}
```

**实体：ScheduledPublish（定时发布）**

```
ScheduledPublish {
  id: Long
  contentId: Long
  scheduledAt: Date
  status: String          // PENDING/EXECUTED/CANCELLED
  createdAt: Date
}
```

### 3.9 数据分析模块

**实体：UserActionLog（用户行为日志）**

```
UserActionLog {
  id: Long
  userId: Long
  actionType: String      // VIEW/SEARCH/FAVORITE/DOWNLOAD/SHARE
  targetType: String      // CONTENT/GROUP/COMMENT
  targetId: Long
  extraData: JSON         // 扩展字段（搜索词、来源页等）
  createdAt: Date
}
```

**实体：ContentStats（内容统计快照）**

```
ContentStats {
  id: Long
  contentId: Long
  viewCount: Long
  favoriteCount: Long
  commentCount: Long
  downloadCount: Long
  statDate: Date
}
```

**约束**：contentId + statDate 联合唯一索引

**实体：SearchHotKeyword（搜索热词）**

```
SearchHotKeyword {
  id: Long
  keyword: String
  searchCount: Int
  statDate: Date
}
```

**约束**：keyword + statDate 联合唯一索引

***

## 4. 功能模块详细设计

### 4.1 用户权限模块

#### 4.1.0 模块概述

用户权限模块是平台的基础安全模块，负责用户的身份认证、权限管理和组织架构管理。支持SSO单点登录和本地账号登录，基于角色的访问控制（RBAC）实现精细化的权限管理。

#### 4.1.0 功能列表

| 序号 | 功能 | 说明 |
|------|------|------|
| 1 | SSO登录 | 支持OAuth2/LDAP协议，与企业SSO系统集成 |
| 2 | 本地账号登录 | 账号密码方式，作为SSO的补充 |
| 3 | Session管理 | 基于Redis的分布式Session管理 |
| 4 | 组织架构 | 部门树形结构，支持LDAP同步 |
| 5 | 角色权限 | 全局角色（普通用户/系统管理员）+ 群组内角色（群主/成员） |
| 6 | 群组权限 | 群主管理群组成员和内容，管理员管理全站 |

#### 4.1.0 业务流程

**业务流程A：用户登录**

```
1. 用户访问平台
       │
       ▼
2. 系统判断登录方式（SSO/本地账号）
       │
       ├─── SSO登录 ──→ 跳转企业SSO认证
       │
       └─── 本地账号 ──→ 输入用户名密码
              │
              ▼
3. 认证成功，生成Session
       │
       ▼
4. 返回JWT Token，后续请求携带Token
```

**业务流程B：权限校验**

```
1. 用户发起操作请求
       │
       ▼
2. 系统解析请求中的JWT Token
       │
       ▼
3. 获取用户角色信息（全局角色 + 群组内角色）
       │
       ▼
4. 根据操作类型判断权限
       │
       ├─── 系统级操作 ──→ 检查是否系统管理员
       │
       └─── 群组内操作 ──→ 检查用户在群组内的角色
              │
              ▼
5. 权限通过，执行操作 / 权限不足，返回错误
```

#### 4.1.1 认证功能

- **SSO 登录**：支持 OAuth2/LDAP 协议，与企业 SSO 系统集成
- **本地账号登录**：账号密码方式，作为 SSO 的补充
- **Session 管理**：基于 Redis 的分布式 Session

#### 4.1.2 组织架构

- 部门树形结构展示
- 支持与企业目录同步（LDAP 同步）

#### 4.1.3 角色权限

**全局角色权限**：

| 权限项 | 普通用户 | 系统管理员 |
|--------|---------|-----------|
| 查看公开内容 | ✅ | ✅ |
| 搜索内容 | ✅ | ✅ |
| 发表评论 | ✅ | ✅ |
| 申请创建群组 | ✅ | ❌ |
| 审批群组创建 | ❌ | ✅ |
| 申请加入群组 | ✅ | ✅ |
| 管理标签 | ❌ | ✅ |
| 管理用户/部门 | ❌ | ✅ |
| 管理敏感词 | ❌ | ✅ |
| 审核内容/评论 | ❌ | ✅ |
| 查看数据看板 | ❌ | ✅ |
| 系统配置 | ❌ | ✅ |

**群组内角色权限**（基于全局角色，叠加群组内权限）：

| 权限项 | 群主（群组内） | 成员（群组内） |
|--------|--------------|--------------|
| 查看群组内容 | ✅ | ✅ |
| 在群组内分享内容 | ✅ | ✅ |
| 审批加入申请 | ✅ | ❌ |
| 管理群组成员 | ✅ | ❌ |
| 管理群组内内容 | ✅ | ❌ |
| 设置群组可见性 | ✅ | ❌ |

**说明**：
- 普通用户可以申请创建群组，由系统管理员审批通过后成为该群组的群主
- 同一用户可以是多个群组的群主（或成员）
- 群主权限仅在其所在的群组内有效

### 4.2 内容发布模块

#### 4.2.0 模块概述

内容发布模块是平台的核心功能，支持用户创建、编辑、发布和管理知识内容。支持多种内容形式（Markdown、PPT、链接、附件），集成标签体系便于分类检索，支持多群组分发。

#### 4.2.0 功能列表

| 序号 | 功能 | 说明 |
|------|------|------|
| 1 | 内容创建 | 创建空白内容（草稿状态） |
| 2 | 富文本编辑 | Markdown编辑器，支持图片/视频/代码块/表格 |
| 3 | PPT上传 | PPT文件上传、下载、在线预览 |
| 4 | 外部链接 | 添加外部URL，自动获取标题摘要 |
| 5 | 内部引用 | 引用平台内其他内容 |
| 6 | 标签选择 | 为内容打标签（支持多标签） |
| 7 | 发布流程 | 草稿→选择群组→设置可见性→发布 |
| 8 | 内容删除 | 软删除，级联清理向量/收藏/文件 |
| 9 | 内容更新 | 修改已发布内容，通知收藏用户 |

#### 4.2.0 业务流程

**业务流程A：创建并发布内容**

```
1. 用户点击"创建内容"
       │
       ▼
2. 系统创建空白草稿
       │
       ▼
3. 用户编辑内容（富文本、PPT、链接等）
       │
       ▼
4. 用户选择标签（可选）
       │
       ▼
5. 用户选择要发布的群组（至少1个）
       │
       ▼
6. 用户设置可见性（公开/群组成员可见）
       │
       ▼
7. 用户点击"发布"
       │
       ▼
8. 系统生成向量数据（Qdrant）
       │
       ▼
9. 内容状态变为"已发布"，通知群组成员
```

**业务流程B：删除内容**

```
1. 用户点击内容详情页的"删除"
       │
       ▼
2. 系统校验权限（作者/群主/管理员）
       │
       ├─── 权限不足 ──→ 返回错误
       │
       ▼ 权限通过
3. 系统执行软删除（标记DELETED）
       │
       ▼
4. 同步删除向量数据
       │
       ▼
5. 同步删除收藏记录
       │
       ▼
6. 同步删除关联文件
       │
       ▼
7. 返回成功，内容从列表消失
```

#### 4.2.1 内容创建与编辑

- Markdown 在线编辑器，支持：
  - 富文本渲染
  - 图片插入（上传至文件服务）
  - 代码块高亮
  - 表格支持
- PPT 文件上传：
  - 方式一：仅存储下载链接
  - 方式二：解析 PPT 内容，支持在线预览
- 外部 URL 链接：输入 URL，自动获取标题和摘要
- 内部引用：选择系统内其他内容进行关联
- **标签选择**：从全局标签库中选择适合的标签（支持多选）

#### 4.2.2 发布流程

1. 创建草稿（自动保存）
2. 编辑内容（包含选择标签）
3. 选择发布的群组（可多选）
4. 设置可见性
5. 发布

#### 4.2.3 文件存储

- 自建文件服务
- 存储介质：本地磁盘 或 MinIO（兼容 S3 协议）
- 文件访问：生成唯一 URL，支持权限校验

#### 4.2.4 内容删除

**删除权限**：
| 角色 | 删除权限 |
|------|---------|
| 作者 | 可删除自己创建的内容 |
| 群主 | 可删除群组内任何内容 |
| 系统管理员 | 可删除任何内容 |

**删除方式**：
- 软删除（标记状态为 DELETED，数据保留，可恢复）

**删除后级联处理**：
| 关联项 | 处理方式 |
|--------|---------|
| 向量数据（Qdrant） | 同步删除 |
| 收藏记录 | 同步删除 |
| 文件（图片/PPT等） | 同步删除 |

**说明**：
- 内容发布后不可撤回/下线，如需移除只能删除
- 删除后内容对所有用户不可见
- 向量、收藏、文件同步清理，避免数据残留

#### 4.2.5 富文本编辑器规格

| 项目 | 规格 |
|------|------|
| **图片上传大小限制** | 单张 ≤ 10MB |
| **图片格式支持** | JPG / PNG / GIF / WebP |
| **视频嵌入** | 支持（仅链接播放，不内嵌视频文件） |
| **代码块语言高亮** | 常用语言（JavaScript / Python / Java / Go / SQL / Shell 等） |
| **附件上传** | 支持（PDF / Word / Excel 等文档格式） |
| **附件大小限制** | 单个 ≤ 20MB |

#### 4.2.6 内容更新通知

- 当已发布内容被作者修改时
- 系统自动通知所有收藏该内容的用户
- 通知类型：`CONTENT_UPDATED`
- 接收人：所有收藏该内容的用户

### 4.3 群组管理模块

#### 4.3.0 模块概述

群组管理模块是平台的内容组织核心，支持用户创建知识分享群组并邀请成员加入。群组采用申请审批制，由系统管理员审批群组创建，群主负责群组成员的日常管理。

#### 4.3.0 功能列表

| 序号 | 功能 | 说明 |
|------|------|------|
| 1 | 群组创建申请 | 普通用户申请创建群组，管理员审批 |
| 2 | 群组发现 | 公开群组列表浏览，申请加入 |
| 3 | 加入审批 | 用户申请加入，群主审批 |
| 4 | 成员管理 | 群主管理成员（移除/退出） |
| 5 | 群主转让 | 群主可转让群组管理权 |
| 6 | 内容分发 | 一篇内容可发布到多个群组 |
| 7 | 可见性管理 | 设置群组内容的可见范围 |
| 8 | 群组信息编辑 | 群主修改群组名称/描述 |
| 9 | 群组解散 | 管理员或群主解散群组 |

#### 4.3.0 业务流程

**业务流程A：创建群组**

```
1. 普通用户填写群组创建申请（名称、描述）
       │
       ▼
2. 系统管理员收到申请通知
       │
       ▼
3. 管理员审批（通过/拒绝）
       │
       ├─── 拒绝 ──→ 通知申请人，流程结束
       │
       ▼ 通过
4. 申请用户成为该群组的群主
       │
       ▼
5. 通知申请人创建成功
```

**业务流程B：加入群组**

```
1. 用户浏览公开群组列表
       │
       ▼
2. 用户点击"申请加入"
       │
       ▼
3. 群主收到入群申请通知
       │
       ▼
4. 群主审批（通过/拒绝）
       │
       ├─── 拒绝 ──→ 通知申请人，流程结束
       │
       ▼ 通过
5. 用户成为群组成员
       │
       ▼
6. 通知申请人加入成功
```

**业务流程C：解散群组**

```
1. 群主/管理员点击"解散群组"
       │
       ▼
2. 系统校验权限
       │
       ▼
3. 群组成员收到解散通知
       │
       ▼
4. 软删除群组（标记DELETED）
       │
       ▼
5. 解散完成
```

#### 4.3.1 群组创建（申请审批制）

**创建流程**：
1. 普通用户填写群组创建申请（名称、描述、期望可见性）
2. 系统管理员收到申请通知
3. 系统管理员审批（通过/拒绝）
4. 审批通过后，申请用户成为该群组的群主

**数据结构**：
```
GroupCreationApplication {
  id: Long
  applicantId: Long      // 申请人ID
  name: String           // 申请群组名称
  description: String   // 申请群组描述
  status: Enum          // PENDING/APPROVED/REJECTED
  reviewerId: Long       // 审批人ID（管理员）
  reviewedAt: Date       // 审批时间
  rejectReason: String   // 拒绝原因（可选）
  createdAt: Date
}
```

#### 4.3.2 加入流程

1. 用户发起加入申请
2. 群主收到通知
3. 群主审批（通过/拒绝）
4. 审批通过后，用户成为群组成员

#### 4.3.3 内容分发

- 一篇内容可同时发布到多个群组
- 内容在各群组中独立管理（删除、置顶等）

#### 4.3.4 可见性管理

- 群主可设置群组内容的可见范围
- 默认：群组内容对所有用户可见
- 可选：仅群组成员可见

### 4.4 标签体系模块

#### 4.4.0 模块概述

标签体系模块提供内容分类和组织能力，支持管理员统一管理全局标签，用户为内容打标签。标签与搜索模块联动，支持按标签筛选和搜索，提升知识发现效率。

#### 4.4.0 功能列表

| 序号 | 功能 | 说明 |
|------|------|------|
| 1 | 标签管理 | 管理员增删改查标签，设置颜色 |
| 2 | 标签使用统计 | 查看标签被多少内容使用 |
| 3 | 内容打标 | 内容发布/编辑时选择标签（多标签） |
| 4 | 标签展示 | 内容页、列表页展示标签 |
| 5 | 标签筛选 | 按标签筛选内容列表 |

#### 4.4.0 业务流程

**业务流程A：标签管理**

```
1. 管理员进入标签管理页面
       │
       ▼
2. 管理员可执行：创建/编辑/删除标签
       │
       ├─── 创建 ──→ 填写标签名称、颜色
       ├─── 编辑 ──→ 修改标签名称、颜色
       └─── 删除 ──→ 确认删除，关联内容解绑
              │
              ▼
3. 保存生效
```

**业务流程B：内容打标**

```
1. 用户在内容编辑页选择标签
       │
       ▼
2. 保存内容时，标签关联同步存储
       │
       ▼
3. 标签使用次数 +1
       │
       ▼
4. 发布后，标签在内容页展示
```

#### 4.4.1 标签管理（管理员）

- 标签增删改查
- 标签颜色设置
- 查看标签使用次数

#### 4.4.2 内容打标

- 内容发布/编辑时可选择标签
- 支持单内容多标签
- 支持添加/移除标签

#### 4.4.3 标签展示

- **内容详情页**：标签以彩色标签组件展示在标题下方，点击可跳转至标签筛选列表
- **内容列表/搜索结果页**：每条内容显示标签徽章
- **标签筛选器**：支持多标签筛选（AND/OR）

### 4.5 收藏模块

#### 4.5.0 模块概述

收藏模块允许用户收藏感兴趣的知识内容，便于日后快速查阅。收藏列表仅用户本人可见，支持按内容类型筛选，提供便捷的知识管理能力。

#### 4.5.0 功能列表

| 序号 | 功能 | 说明 |
|------|------|------|
| 1 | 收藏内容 | 收藏感兴趣的内容，防重复 |
| 2 | 取消收藏 | 取消已收藏的内容 |
| 3 | 收藏列表 | 查看已收藏内容，支持筛选 |
| 4 | 收藏统计 | 内容详情页显示收藏数 |

#### 4.5.0 业务流程

**业务流程A：收藏内容**

```
1. 用户在内容详情页点击"收藏"按钮
       │
       ▼
2. 系统校验是否已收藏
       │
       ├─── 已收藏 ──→ 提示"已收藏"
       │
       ▼ 未收藏
3. 创建收藏记录
       │
       ▼
4. 更新内容收藏数 +1
       │
       ▼
5. 收藏成功，按钮变为"已收藏"
```

**业务流程B：取消收藏**

```
1. 用户在内容详情页点击"已收藏"按钮
       │
       ▼
2. 删除收藏记录
       │
       ▼
3. 更新内容收藏数 -1
       │
       ▼
4. 取消成功，按钮变为"收藏"
```

#### 4.5.1 收藏操作

- 收藏/取消收藏内容
- 收藏时校验是否已收藏（防重复）

#### 4.5.2 收藏列表

- 按收藏时间倒序展示
- 支持按内容类型筛选（Markdown/PPT/链接）
- **支持点击跳转至内容详情页阅读**
- 支持取消收藏

#### 4.5.3 收藏统计

- 内容详情页显示总收藏数（非当前用户收藏数）

### 4.6 消息通知模块

#### 4.6.0 模块概述

消息通知模块负责向用户推送站内通知，涵盖内容、群组、系统等多种场景。用户可在顶部导航栏查看未读通知，在通知中心管理历史通知，确保重要信息及时触达。

#### 4.6.0 功能列表

| 序号 | 功能 | 说明 |
|------|------|------|
| 1 | 通知触发 | 内容发布、评论回复、入群申请、群组创建、系统公告等场景 |
| 2 | 通知展示 | 顶部导航栏Badge + 通知列表页 |
| 3 | 通知跳转 | 点击通知跳转到对应详情页 |
| 4 | 通知管理 | 标记已读、删除通知 |

#### 4.6.0 业务流程

**业务流程A：通知生成与推送**

```
1. 业务操作触发通知事件
       │
       ▼
2. 系统创建通知记录（类型、接收人、关联实体）
       │
       ▼
3. 通知入库，标记为未读
       │
       ▼
4. 接收人顶部导航栏未读数 +1，显示红点
```

**业务流程B：查看并处理通知**

```
1. 用户点击导航栏通知图标
       │
       ▼
2. 展示最新5条通知
       │
       ▼
3. 用户点击"查看全部"进入通知中心
       │
       ▼
4. 用户可执行：标记已读、全部已读、删除
       │
       ▼
5. 操作结果同步更新
```

#### 4.6.1 通知触发场景

| 场景 | 触发时机 | 接收人 |
|------|---------|--------|
| 内容发布 | 用户发布内容成功后 | 发布者本人（确认发布成功） |
| 评论/回复 | 其他用户评论/回复时 | 内容作者、评论被回复者 |
| 评论@提及 | 评论中@其他用户时 | 被@的用户 |
| 入群申请 | 用户提交入群申请时 | 群主 |
| 入群申请通过 | 群主审批通过时 | 申请人 |
| 入群申请拒绝 | 群主审批拒绝时 | 申请人 |
| 群组创建申请 | 用户提交群组创建申请时 | 系统管理员 |
| 群组创建申请通过 | 管理员审批通过时 | 申请人（成为群主） |
| 群组创建申请拒绝 | 管理员审批拒绝时 | 申请人 |
| 内容更新 | 收藏的内容有更新时 | 收藏该内容的用户 |
| 内容审核通过 | 管理员审核通过时 | 内容作者 |
| 内容审核驳回 | 管理员审核驳回时 | 内容作者 |
| 系统公告 | 管理员发布公告时 | 全体用户 |

#### 4.6.2 通知展示

- **全局顶部导航栏**：通知铃铛图标 + 未读数量红点Badge，点击展开通知列表面板（最新5条）
- **通知列表页（通知中心）**：
  - Tab 分类：全部 / 评论回复 / 群组 / 系统
  - 每条通知显示：类型图标 + 标题 + 内容摘要 + 时间 + 未读红点
  - 支持左滑/右键删除
  - 支持标记单条/全部已读

#### 4.6.3 通知跳转

点击通知跳转到对应详情页：

- 内容相关 → 内容详情页
- 群组申请 → 群组审批页
- 审核相关 → 审核详情页
- 系统公告 → 公告详情页

#### 4.6.4 通知管理

- 保留全部历史通知
- 用户可手动删除或标记已读

### 4.7 搜索服务模块

#### 4.7.0 模块概述

搜索服务模块提供高效的知识检索能力，融合全文检索（Elasticsearch）和语义搜索（向量检索）。支持关键词精确匹配和语义理解，结合标签筛选和多维度排序，帮助用户快速找到所需知识。

#### 4.7.0 功能列表

| 序号 | 功能 | 说明 |
|------|------|------|
| 1 | 全文检索 | 基于Elasticsearch的关键词匹配、分词、模糊搜索 |
| 2 | 语义搜索 | 基于BGE向量模型的语义相似度检索 |
| 3 | 混合检索 | RRF算法融合关键词和向量排名 |
| 4 | 标签筛选 | 支持按标签过滤搜索结果 |
| 5 | 多维度排序 | 按时间、相关性、热度、标签匹配度排序 |
| 6 | 搜索建议 | 搜索词补全提示 |
| 7 | 向量重建 | 管理后台手动触发批量向量重建 |

#### 4.7.0 业务流程

**业务流程A：混合检索**

```
1. 用户输入搜索词
       │
       ▼
2. 系统同时执行：
   - 关键词检索（ES）→ 排名列表A
   - 向量检索（Qdrant）→ 排名列表B
       │
       ▼
3. RRF算法融合两个排名列表
       │
       ▼
4. 按综合得分排序，返回结果
       │
       ▼
5. 高相关性结果优先展示
```

**业务流程B：向量生成**

```
1. 内容发布成功
       │
       ▼
2. 调用BGE模型生成向量
       │
       ▼
3. 向量存入Qdrant
       │
       ▼
4. 搜索时该内容可被向量检索命中
```

**业务流程C：批量重建**

```
1. 管理员在管理后台触发重建
       │
       ▼
2. 系统分批获取待重建内容
       │
       ▼
3. 逐条调用BGE模型重新生成向量
       │
       ▼
4. 更新Qdrant中的向量数据
       │
       ▼
5. 重建完成
```

#### 4.7.1 全文检索（Elasticsearch）

- **索引内容**：标题 + 正文全文
- **分词**：中文分词器（如 IK Analyzer）
- **搜索方式**：
  - 关键词精确匹配
  - 模糊匹配
  - 短语匹配

#### 4.7.2 语义搜索（向量检索）

##### 4.7.2.1 Embedding 生成

| 项目        | 方案                      |
| --------- | ----------------------- |
| **模型**    | BGE-base-zh（768维向量）     |
| **模型特点**  | 中文优化，支持私有部署，无需调用大模型API  |
| **生成时机**  | 内容发布时同步生成               |
| **生成内容**  | 标题 + 正文                 |
| **冷启动处理** | 纯关键词兜底（新内容暂无双向量，用关键词匹配） |

##### 4.7.2.2 向量存储（Qdrant）

| 项目          | 配置                |
| ----------- | ----------------- |
| **向量维度**    | 768维              |
| **TopK**    | 50                |
| **相似度阈值**   | 不设限（返回所有TopK结果）   |
| **内容更新/删除** | 删除旧向量，更新时下次搜索触发重建 |

##### 4.7.2.3 混合检索（RRF）

**检索流程：**

```
用户查询
    │
    ├──→ 关键词检索（Elasticsearch）──→ 获得排名列表A
    │
    └──→ 向量检索（Qdrant）────────→ 获得排名列表B
              │
              ▼
        RRF融合公式
        RRF_score = Σ 1/(k+rank_i)  （k=60）
              │
              ▼
        综合排名结果
```

**说明**：

- RRF（Reciprocal Rank Fusion）是业界成熟的混合检索融合算法
- 关键词检索保证精确匹配，向量检索保证语义理解
- 两者互补，提供更准确的搜索结果

##### 4.7.2.4 批量重建机制

| 功能       | 说明             |
| -------- | -------------- |
| **触发方式** | 管理后台手动触发       |
| **使用场景** | 模型升级、内容向量损坏/丢失 |
| **处理方式** | 分批重建，避免一次性压力   |

##### 4.7.2.5 性能指标

| 指标      | 要求         |
| ------- | ---------- |
| 向量生成    | 单条 ≤ 500ms |
| 向量检索响应  | ≤ 1.5秒     |
| 混合检索总响应 | ≤ 3秒       |

#### 4.7.3 标签搜索增强

- 搜索API新增标签过滤参数：`GET /api/search?keyword=xxx&tags=1,2,3&sort=relevance`
- `tags`：标签ID列表，支持多标签筛选
- 搜索结果按关键词相关性 + 标签匹配度综合排序

#### 4.7.4 搜索排序

- 用户可自定义排序规则：
  - 按发布时间（最新/最旧）
  - 按相关性（ES 评分 + 向量相似度融合）
  - 按热度（浏览量、点赞数、收藏数）
  - **按标签匹配度**

#### 4.7.5 性能要求

- 响应时间 ≤ 3 秒

### 4.8 评论互动模块

#### 4.8.0 模块概述

评论互动模块为知识内容提供讨论和交流能力，支持嵌套回复、点赞、举报、审核和敏感词过滤。通过 @提及功能增强用户互动，通过审核机制和敏感词过滤保障内容安全合规。

#### 4.8.0 功能列表

| 序号 | 功能 | 说明 |
|------|------|------|
| 1 | 评论发布 | 支持富文本评论，嵌套回复（二级结构） |
| 2 | @提及 | 评论中@其他用户，自动通知 |
| 3 | 评论点赞 | 点赞/取消点赞评论 |
| 4 | 评论举报 | 举报不当评论 |
| 5 | 评论审核 | 管理员审核评论（通过/驳回/隐藏） |
| 6 | 敏感词过滤 | 评论发布时自动检测和过滤敏感词 |
| 7 | 评论排序 | 按热度（点赞数）/时间排序 |

#### 4.8.0 业务流程

**业务流程A：发表评论**

```
1. 用户在内容详情页输入评论
       │
       ▼
2. 系统检测敏感词
       │
       ├─── 含敏感词 ──→ 提示用户修改
       │
       ▼ 通过检测
3. 系统保存评论（auditStatus=PENDING）
       │
       ▼
4. 发送 CommentCreatedEvent（MQ）
       │
       ├──→ 通知内容作者
       ├──→ 通知被@的用户
       └──→ 记录行为日志
       │
       ▼
5. 管理员审核（通过/驳回）
       │
       ├─── 驳回 ──→ 通知评论者
       │
       ▼ 通过
6. 评论公开可见
```

**业务流程B：评论举报处理**

```
1. 用户点击评论的"举报"按钮
       │
       ▼
2. 系统记录举报信息
       │
       ▼
3. 管理员在审核中心查看被举报评论
       │
       ▼
4. 管理员处理（忽略/隐藏评论/删除评论）
```

#### 4.8.1 评论结构

- **一级评论**：直接对内容的评论
- **二级回复**：对一级评论的回复（不嵌套更深层级）
- 回复时显示"回复 @用户名"

#### 4.8.2 敏感词过滤

- 基于 Trie 树（AC 自动机）实现高效敏感词匹配
- 支持全词匹配和拼音匹配
- 管理员可增删敏感词，支持批量导入
- 检测到敏感词时前端实时高亮提示，后端二次校验

#### 4.8.3 评论审核

- 评论发布后默认进入待审核状态
- 管理员可在审核中心查看待审核评论
- 审核操作：通过（公开可见）、驳回（通知作者修改）、隐藏（仅作者可见）

#### 4.8.4 评论展示

- 内容详情页底部展示评论区
- 默认按热度排序（点赞数降序），支持切换为时间排序
- 每条评论显示：头像、用户名、时间、正文、点赞按钮、回复按钮、举报按钮
- 被驳回/隐藏的评论对普通用户不可见

### 4.9 内容生产工具链模块

#### 4.9.0 模块概述

内容生产工具链模块为内容创作者提供专业的生产辅助能力，包括历史版本管理、审核工作流、内容模板和定时发布。帮助用户更高效地创作和管理知识内容，保障内容质量。

#### 4.9.0 功能列表

| 序号 | 功能 | 说明 |
|------|------|------|
| 1 | 历史版本管理 | 自动保存编辑历史，支持版本对比和回滚 |
| 2 | 审核工作流 | 内容提交→管理员审核→通过发布/驳回修改 |
| 3 | 内容模板 | 预设模板，快速创建标准化内容 |
| 4 | 定时发布 | 设置未来时间自动发布内容 |

#### 4.9.0 业务流程

**业务流程A：版本管理**

```
1. 用户编辑已发布内容并保存
       │
       ▼
2. 系统自动创建新版本快照
       │
       ▼
3. 用户可在版本历史页查看所有版本
       │
       ▼
4. 用户可选择两个版本进行差异对比
       │
       ▼
5. 用户可选择回滚到某个历史版本
       │
       ▼
6. 回滚后创建新版本（记录回滚操作）
```

**业务流程B：审核工作流**

```
1. 用户完成内容编辑，点击"提交审核"
       │
       ▼
2. 内容状态变为 PENDING_AUDIT
       │
       ▼
3. 管理员在审核中心看到待审核内容
       │
       ▼
4. 管理员审核（通过/驳回）
       │
       ├─── 驳回 ──→ 填写驳回原因 → 通知作者修改
       │
       ▼ 通过
5. 内容状态变为 PUBLISHED，执行发布流程
       │
       ▼
6. 通知作者审核通过
```

**业务流程C：定时发布**

```
1. 用户编辑内容，设置定时发布时间
       │
       ▼
2. 系统创建定时发布任务
       │
       ▼
3. 定时任务调度器扫描到期任务
       │
       ▼
4. 到达设定时间，自动执行发布流程
       │
       ▼
5. 发布成功后通知作者
```

#### 4.9.1 历史版本管理

- 每次内容更新自动保存版本快照（标题 + 正文）
- 版本号自增，记录变更摘要
- 版本对比：双栏 diff 视图，高亮显示增删改内容
- 版本回滚：选择历史版本恢复，回滚操作本身也创建新版本

#### 4.9.2 审核工作流

- 审核范围：内容发布审核 + 评论审核
- 审核状态流转：`PENDING → APPROVED / REJECTED`
- 驳回时必填驳回原因
- 审核记录持久化，支持追溯

#### 4.9.3 内容模板

- 系统预置常用模板（技术文档、产品说明、会议纪要等）
- 用户可创建个人模板
- 从模板创建内容时自动填充结构和格式

#### 4.9.4 定时发布

- 支持设置未来任意时间点发布
- 到达时间后自动执行发布流程（生成向量、通知群组等）
- 支持取消未执行的定时任务
- 基于定时任务调度（如 Quartz / XXL-Job）

### 4.10 数据分析模块

#### 4.10.0 模块概述

数据分析模块为管理员提供平台运营数据看板，帮助了解内容生产趋势、用户活跃度和搜索热点。通过定时汇总统计快照和实时行为日志采集，提供多维度的数据可视化。

#### 4.10.0 功能列表

| 序号 | 功能 | 说明 |
|------|------|------|
| 1 | 平台总览 | 总内容数、总用户数、今日发布量等核心指标 |
| 2 | 内容趋势 | 内容发布量趋势图（按日/周/月） |
| 3 | 热门内容 | 热门内容 Top N（按阅读量/收藏量） |
| 4 | 搜索热词 | 用户搜索关键词排行 |
| 5 | 群组活跃度 | 群组内容量和成员活跃度排行 |

#### 4.10.0 业务流程

**业务流程A：行为数据采集**

```
1. 用户在前端执行操作（浏览/搜索/收藏/下载）
       │
       ▼
2. 前端发送埋点请求到后端
       │
       ▼
3. 后端发送 UserActionEvent（MQ）
       │
       ▼
4. analytics 模块消费事件，写入行为日志表
       │
       ▼
5. 定时任务汇总日志到统计快照表
```

**业务流程B：数据看板查询**

```
1. 管理员进入数据分析看板页面
       │
       ▼
2. 前端请求各维度统计数据
       │
       ▼
3. 后端查询统计快照表（预聚合数据）
       │
       ▼
4. 返回数据，前端 ECharts 渲染图表
```

#### 4.10.1 平台总览

- 核心指标卡片：总内容数、总用户数、今日发布量、今日活跃用户数
- 数据来源：统计快照表 + 实时查询

#### 4.10.2 内容趋势

- 折线图展示内容发布量随时间变化趋势
- 支持按日/周/月切换粒度
- 可按内容类型（Markdown/PPT/链接）分组展示

#### 4.10.3 热门内容

- 排行榜展示 Top 20 热门内容
- 排序维度：阅读量、收藏量、评论数
- 点击可跳转至内容详情页

#### 4.10.4 搜索热词

- 排行榜展示 Top 20 搜索关键词
- 展示搜索次数和趋势变化
- 数据来源：搜索热词统计表

#### 4.10.5 群组活跃度

- 排行榜展示群组活跃度
- 排序维度：内容总量、近期发布量、成员数
- 点击可跳转至群组详情页

***

## 5. API 设计概要

### 5.1 用户权限 API

| 接口                       | 方法   | 说明       |
| ------------------------ | ---- | -------- |
| `/api/auth/login`        | POST | 账号密码登录   |
| `/api/auth/sso/callback` | GET  | SSO 回调   |
| `/api/users/me`          | GET  | 获取当前用户信息 |
| `/api/departments`       | GET  | 获取部门树    |

### 5.2 内容发布 API

| 接口                           | 方法     | 说明       |
| ---------------------------- | ------ | -------- |
| `/api/contents`              | POST   | 创建内容（草稿） |
| `/api/contents/{id}`         | GET    | 获取内容详情   |
| `/api/contents/{id}`         | PUT    | 更新内容     |
| `/api/contents/{id}`         | DELETE | 删除内容     |
| `/api/contents/{id}/publish` | POST   | 发布内容     |
| `/api/contents/{id}/draft`   | POST   | 保存草稿     |

### 5.3 群组管理 API

| 接口                                  | 方法     | 说明     |
| ----------------------------------- | ------ | ------ |
| `/api/groups`                       | POST   | 创建群组   |
| `/api/groups`                       | GET    | 获取群组列表 |
| `/api/groups/{id}`                  | GET    | 获取群组详情 |
| `/api/groups/{id}/join`             | POST   | 申请加入   |
| `/api/groups/{id}/members`          | GET    | 获取成员列表 |
| `/api/groups/{id}/members/{userId}` | PUT    | 审批成员   |
| `/api/groups/{id}/members/{userId}` | DELETE | 移除成员   |

### 5.4 标签体系 API

| 接口                                | 方法     | 说明        |
| --------------------------------- | ------ | --------- |
| `/api/tags`                       | GET    | 获取所有标签列表  |
| `/api/tags`                       | POST   | 创建标签（管理员） |
| `/api/tags/{id}`                  | PUT    | 更新标签（管理员） |
| `/api/tags/{id}`                  | DELETE | 删除标签（管理员） |
| `/api/contents/{id}/tags`         | GET    | 获取内容的所有标签 |
| `/api/contents/{id}/tags`         | POST   | 为内容添加标签   |
| `/api/contents/{id}/tags/{tagId}` | DELETE | 移除内容的某个标签 |

### 5.5 收藏 API

| 接口                                 | 方法     | 说明             |
| ---------------------------------- | ------ | -------------- |
| `/api/favorites`                   | GET    | 获取当前用户的收藏列表    |
| `/api/favorites/{contentId}`       | POST   | 收藏某个内容         |
| `/api/favorites/{contentId}`       | DELETE | 取消收藏           |
| `/api/favorites/check/{contentId}` | GET    | 检查当前用户是否已收藏某内容 |

### 5.6 消息通知 API

| 接口                                | 方法     | 说明              |
| --------------------------------- | ------ | --------------- |
| `/api/notifications`              | GET    | 获取通知列表（分页，支持筛选） |
| `/api/notifications/unread-count` | GET    | 获取未读通知数量        |
| `/api/notifications/{id}/read`    | PUT    | 标记单条为已读         |
| `/api/notifications/read-all`     | PUT    | 全部标记为已读         |
| `/api/notifications/{id}`         | DELETE | 删除通知            |

### 5.7 搜索 API

| 接口                    | 方法  | 说明               |
| --------------------- | --- | ---------------- |
| `/api/search`         | GET | 全文 + 语义搜索 + 标签筛选 |
| `/api/search/suggest` | GET | 搜索建议/补全          |

### 5.8 评论互动 API

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/contents/{contentId}/comments` | GET | 获取评论列表（分页，支持按热度/时间排序） |
| `/api/contents/{contentId}/comments` | POST | 发表评论（支持 @提及、富文本） |
| `/api/comments/{id}` | PUT | 编辑评论（仅作者） |
| `/api/comments/{id}` | DELETE | 删除评论（软删除，作者/管理员） |
| `/api/comments/{id}/like` | POST | 点赞评论 |
| `/api/comments/{id}/like` | DELETE | 取消点赞 |
| `/api/comments/{id}/report` | POST | 举报评论 |

### 5.9 内容生产工具链 API

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/contents/{id}/versions` | GET | 获取历史版本列表 |
| `/api/contents/{id}/versions/{versionId}` | GET | 获取指定版本详情 |
| `/api/contents/{id}/versions/{versionId}/diff` | GET | 版本差异对比（与当前版本） |
| `/api/contents/{id}/versions/{versionId}/rollback` | POST | 回滚到指定版本 |
| `/api/contents/{id}/submit-audit` | POST | 提交审核 |
| `/api/audit/pending` | GET | 待审核列表（审核人视角，支持按类型筛选） |
| `/api/audit/{id}/approve` | POST | 审核通过 |
| `/api/audit/{id}/reject` | POST | 审核驳回（含原因） |
| `/api/templates` | GET | 获取模板列表 |
| `/api/templates` | POST | 创建模板 |
| `/api/templates/{id}` | PUT | 编辑模板 |
| `/api/templates/{id}` | DELETE | 删除模板 |
| `/api/contents/{id}/schedule` | POST | 设置定时发布 |
| `/api/contents/{id}/schedule` | DELETE | 取消定时发布 |

### 5.10 数据分析 API

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/analytics/overview` | GET | 平台总览（总内容数、总用户数、今日发布量等） |
| `/api/analytics/content-trend` | GET | 内容发布趋势（按日/周/月，支持按类型分组） |
| `/api/analytics/hot-content` | GET | 热门内容 Top N（按阅读量/收藏量/评论数） |
| `/api/analytics/hot-keywords` | GET | 搜索热词排行 |
| `/api/analytics/group-activity` | GET | 群组活跃度排行 |

### 5.11 敏感词管理 API

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/sensitive-words` | GET | 敏感词列表（分页） |
| `/api/sensitive-words` | POST | 添加敏感词（管理员） |
| `/api/sensitive-words/{id}` | DELETE | 删除敏感词（管理员） |
| `/api/sensitive-words/batch` | POST | 批量导入敏感词（管理员） |

***

## 6. 安全设计

### 6.1 认证与授权

- JWT Token 认证
- 基于角色的访问控制（RBAC）
- API 级别权限校验

### 6.2 文件安全

- 上传文件类型校验
- 文件大小限制
- 访问 URL 时效性校验

### 6.3 数据安全

- 敏感数据加密存储
- 日志审计
- 敏感词过滤（评论 + 内容）

***

## 7. 部署架构

### 7.1 部署模式

- 支持私有化部署和云端部署
- 容器化部署（Docker）

### 7.2 基础服务依赖

- MySQL 8.0+
- Redis 6.0+
- Elasticsearch 8.x
- Qdrant（向量数据库）
- RabbitMQ（消息队列）
- MinIO（可选，文件存储）

***

## 8. 前端页面结构

### 8.1 页面清单

#### 内容相关

| 页面 | 路由 | 说明 |
|------|------|------|
| 内容列表（首页） | `/` | 公开内容流，支持标签筛选、排序 |
| 内容详情 | `/content/:id` | 阅读正文 + 评论区 + 收藏按钮 |
| 内容编辑 | `/content/:id/edit` | Markdown 编辑器 + 标签选择 + 群组选择 |
| 内容创建 | `/content/create` | 新建内容 |
| 版本历史 | `/content/:id/versions` | 版本列表 + 差异对比 + 回滚 |
| 模板中心 | `/templates` | 模板浏览 + 从模板创建 |

#### 群组相关

| 页面 | 路由 | 说明 |
|------|------|------|
| 群组列表 | `/groups` | 公开群组浏览 + 搜索 |
| 群组详情 | `/group/:id` | 群组内容流 + 成员列表 |
| 群组管理 | `/group/:id/manage` | 成员审批 + 可见性设置 + 群主转让 |

#### 审核相关

| 页面 | 路由 | 说明 |
|------|------|------|
| 审核中心 | `/audit` | 待审核列表（内容审核 + 评论审核） |
| 审核详情 | `/audit/:id` | 审核详情 + 通过/驳回操作 |

#### 用户相关

| 页面 | 路由 | 说明 |
|------|------|------|
| 登录 | `/login` | SSO + 本地账号 |
| 个人收藏 | `/favorites` | 收藏列表 + 类型筛选 |
| 通知中心 | `/notifications` | 通知列表 + 分类 Tab + 已读管理 |

#### 管理后台

| 页面 | 路由 | 说明 |
|------|------|------|
| 标签管理 | `/admin/tags` | 标签 CRUD + 颜色设置 |
| 用户管理 | `/admin/users` | 用户列表 + 角色分配 |
| 部门管理 | `/admin/departments` | 部门树管理 |
| 敏感词管理 | `/admin/sensitive-words` | 敏感词 CRUD + 批量导入 |
| 数据分析看板 | `/admin/analytics` | 运营数据看板（图表展示） |
| 系统配置 | `/admin/settings` | 系统参数配置 |

### 8.2 核心交互组件

| 组件 | 说明 |
|------|------|
| `CommentSection` | 评论区组件（嵌套回复、点赞、举报、@提及选择器） |
| `CommentInput` | 评论输入框（富文本、@提及自动补全） |
| `VersionDiff` | 版本差异对比组件（双栏 diff 视图） |
| `AuditActionBar` | 审核操作栏（通过/驳回按钮 + 驳回原因输入） |
| `AnalyticsChart` | 数据图表组件（基于 ECharts，折线图/柱状图/饼图） |
| `SensitiveWordChecker` | 敏感词实时检测提示（输入时高亮） |
| `TagSelector` | 标签多选组件 |
| `MarkdownEditor` | Markdown 编辑器 |
| `FileUploader` | 文件上传组件 |
| `NotificationBell` | 顶部通知铃铛 + 未读 Badge |

***

## 9. 交付清单

### P0 必须交付

- [ ] 用户认证（SSO + 本地账号）
- [ ] 组织架构管理
- [ ] 角色权限体系
- [ ] Markdown 内容发布
- [ ] PPT 文件上传/预览/下载
- [ ] 外部链接发布
- [ ] 内部引用链接
- [ ] 草稿/发布流程
- [ ] 群组创建与管理
- [ ] 加入群组审批流程
- [ ] 内容分发到多个群组
- [ ] 全文检索（Elasticsearch）
- [ ] 语义搜索（向量检索）
- [ ] 自定义排序
- [ ] 文件存储服务
- [ ] 标签体系（管理员管理 + 内容打标 + 标签搜索）
- [ ] 收藏功能（收藏/取消 + 收藏列表 + 跳转阅读）
- [ ] 消息通知系统（站内通知 + 多场景触发 + 通知管理）
- [ ] **评论互动系统（评论发布/回复 + 点赞 + 举报 + 审核 + 敏感词过滤 + @提及）**
- [ ] **历史版本管理（版本快照 + 差异对比 + 回滚）**
- [ ] **审核工作流（内容审核 + 评论审核）**
- [ ] **内容模板系统**
- [ ] **定时发布功能**
- [ ] **数据分析看板（内容趋势 + 热门排行 + 搜索热词 + 群组活跃度）**
- [ ] **敏感词管理**
- [ ] **DDD 模块化架构（7 个业务模块 + 四层分层）**
- [ ] **消息队列异步解耦（6 个 Topic + 可靠性保障）**

***

**修订记录**：

| 日期 | 版本 | 修订内容 |
|------|------|---------|
| 2026-04-25 | v1.0 | 初始版本 |
| 2026-04-25 | v2.0 | 新增标签体系、收藏功能、消息通知系统 |
| 2026-04-25 | v2.1 | 细化语义搜索：BGE-base-zh模型+混合检索RRF+批量重建机制 |
| 2026-04-25 | v2.2 | 重构角色体系：区分全局角色（普通用户/系统管理员）与群组内角色（群主/成员），群组创建需管理员审批 |
| 2026-04-25 | v2.3 | 细化内容发布模块：新增内容删除规范、富文本编辑器规格、内容更新通知 |
| 2026-04-25 | v2.4 | 补充各模块概述、功能列表、业务流程（用户权限、内容发布、群组管理、标签体系、收藏、消息通知、搜索服务） |
| 2026-05-07 | v3.0 | 重大升级：新增评论互动模块（4.8）、内容生产工具链模块（4.9）、数据分析模块（4.10）；新增 DDD 模块化架构设计（2.3）；新增消息队列事件设计（2.4）；新增 RabbitMQ 技术选型；新增敏感词管理 API（5.11）；新增前端页面结构（8）；更新交付清单（9）；更新通知类型枚举（COMMENT_MENTION、CONTENT_AUDIT_APPROVED、CONTENT_AUDIT_REJECTED） |
